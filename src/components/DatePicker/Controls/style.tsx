import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  justify-content: space-between;
  padding: 0 0 10px 0;
  align-items: center;

  h4 {
    margin: 0;
    padding: 0 20px;
    font-size: 0.8em;
  }

  button {
    cursor: pointer;
  }
`;
